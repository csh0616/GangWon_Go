# agent/는 server/의 lib를 상대경로로 import한다. dotenv가 실행 cwd(agent/) 기준으로 .env를 찾으므로
# server/.env와 값이 겹치더라도 여기에도 동일하게 채워야 한다 (agent/README.md 참고).

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

KAKAO_MOBILITY_API_KEY=
KMA_SERVICE_KEY=
